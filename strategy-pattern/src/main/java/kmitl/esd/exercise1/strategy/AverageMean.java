package kmitl.esd.exercise1.strategy;

/**
 * Implements Average Interface
 */
public class AverageMean implements Average {
    /**
     * returns MEAN average
     * @param val the values that are involved in calculating average mean.
     * @return MEAN.
     */
    @Override
    public float getAverage(int[] val) {
        return 25.5f;
    }
}
