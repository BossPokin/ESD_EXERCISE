package kmitl.esd.exercise1.strategy;

/**
 * A strategy class  that implements Average interface
 */
public class AverageMedian implements Average {
    /**
     * returns average MEDIAN
     * @param val the values that are involved in calculating average median.
     * @return MEDIAN.
     */
    @Override
    public float getAverage(int[] val) {
        return 35.5f;
    }
}
